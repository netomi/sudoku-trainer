allprojects {
    group 'org.netomi.sudoku'
    version '1.0-SNAPSHOT'

    repositories {
        mavenCentral()
        jcenter()
    }
}

subprojects {
    apply plugin: "java"

    sourceCompatibility = 1.9
    targetCompatibility = 1.9

    ext {
        junitVersion = "5.3.2"
    }
}